# Tasks — spark-remote-inference

Status: Implemented and live-verified; security release evidence unavailable

Startup extension: Implemented and verified; explicit user request for one-command connection.

Authority: [Accepted PRD](PRD.md), [plan](plan.md).

- [x] T01 Record the user's explicit remote Spark request and retain the fixed stack/private model boundary.
- [x] T02 Establish authorized SSH; inspect the device and actual Ethernet route. Windows 192.168.137.1 reaches Spark 192.168.137.2; Wi-Fi remains intact. Docker and native CDI access are verified.
- [x] T03 Prepare and admit the separate ARM64 runtime; verify the pinned model and real CUDA inference. The native offline build, imports/matrix operation and nine real HTTP smoke checks passed with the same 192-dimensional model revision.
- [x] T04 Implement and verify SSH/Compose transport and explicit local/remote selection. The owned tunnel checks the host key, process identity and model readiness. Both private relays are verified. A four-worker regression exposed IPv6 host selection; the final proxy derives a unique IPv4 at startup and passed 50 consecutive POST requests without retries.
- [x] T05 Run a genuine application job on Spark; stop local inference only after remote validation; cover failure and restart. The first job UUID is present in the Spark log. A tunnel outage terminated after two attempts without fallback; new jobs passed after reconnection and after restarting the Spark service. Existing profiles were unchanged. Windows startup successfully selected Spark and kept local inference stopped.
- [ ] T06 Complete applicable automated/deployment gates, record numerical/performance evidence and every unavailable check. The 263 focused tests and full 80-test application gate passed; security execution remains unavailable because the admitted offline scanner bundle is absent. Numerical, 20-job performance, 91-sample shared-memory and all missing-evidence records are complete.
- [x] T07 Add the one-click Windows coordinator and bounded prepared-service starter on Spark; cover local endpoint, Ethernet, failure, ownership and duplicate launch boundaries. 81 tests passed/1 Windows symlink skip; three real Linux filesystem checks passed separately.
- [x] T08 Verify the single entry point live, including service recovery and idempotent repeat; update usage documentation and full-gate evidence without claiming an automatic cable-event watcher. CMD recovered only the stopped project services, reused the owned tunnel on repeat, and a real application job succeeded on Spark. Profiles and two other containers were unchanged. Full 80-test application gate passed; security retry exited 2 because offline gitleaks is absent. See the one-click run report for unexecuted cold-Desktop/reboot/browser cases.
- [x] T09 Fix the observed stale-nginx-address and console-dependent native stdin regressions. The startup/local configuration suites passed 55 cases, the final native coordinator suite passed 44, and an isolated real-nginx replacement regression passed once; nine cases are newly added. See the public evaluation's nginx reload report for preserved red and green evidence.
- [x] T10 Verified the updated single-click launcher after the blind evaluation: PowerShell 7 and Windows PowerShell 5.1 both returned ready, with Spark CUDA ready and local inference stopped. The post-startup full profile gate passed 69 backend and 21 frontend tests, plus configuration/contracts/schema/governance/charts. Original profiles and other projects were unchanged. See `docs/evidence/2026-09-09-public-speaker-evaluation/final-live-environment.json` and `quality-gate-post-startup.txt`; missing admitted security/browser packages still prevent full release acceptance under T06.

Evidence is recorded in [the runtime report](../../../../docs/evidence/2026-09-09-spark-runtime/README.md) and [the one-click report](../../../../docs/evidence/2026-09-09-spark-one-click/README.md), with separate native, tunnel, startup and transport reports. The earlier [preflight report](../../../../docs/evidence/2026-09-09-spark-preflight/README.md) is historical preparation evidence; its then-pending device actions have now been observed.

T06 observations: The single-fixture cross-device cosine similarity is 0.9999999525; no accuracy or predefined tolerance pass is inferred. The first application benchmark was aborted because one job retried through an unreachable IPv6 host address; that failed attempt is preserved. After correction, 20/20 measured jobs completed on their first attempt: worker execution p95 0.198461 seconds, queue-inclusive server p95 2.111148 seconds. Available whole-host memory stayed above 20.38 GiB in the 90-second sample; this is not model-only memory. The latest security gate retry exited 2 because the admitted offline gitleaks tool is unavailable; no security pass is claimed.

Scope limits: The pilot's 120-second/50-MiB limit remains. Long recordings and live streams belong to Draft capabilities 002/003. Natural Turkish multi-person accuracy and owner acceptance remain unmeasured. Native Torch warns that its compiled architecture list ends at SM120 while GB10 reports SM121; the observed real model checks passed, but do not establish all-kernel compatibility.
