# Koşum raporu — 2026-09-11 · Bağımsız iki modelle karışık ses profilini engelleme

1. Sonuç: Sabit 85 ses kontrolü geçti; önceki karışık örnek hem yalıtılmış modelde hem çalışan yeni imajın gerçek HTTP ucunda saklanabilir çıktı üretmeden reddedildi — birim 221 başarılı / gerçek GPU 85 başarılı / ayrı kullanıcı kaydı 1 başarılı / çalışan özel HTTP 1 başarılı / tarayıcı 0 başarılı / atlanan 0; karar bekleyen: yok.
2. Koşulan: `kt-vibecoding-python-web-v2`, çevrimdışı Python 3.13.14 ve RTX 4060; yeni sözleşmeler RED 14 başarısız/2 başarılı → GREEN 16 başarılı, tüm çıkarım paketi 221 başarılı. İlk adayda hash ile eşleşen önceki 56 sonuç ve yeni 29 GPU kaydı kullanıldı; ardından 85 kaydın tamamı gerçek üretim fonksiyonundan geçti. Ayrı tamamlayıcı koşumda gerçek GPU/ASGI hafıza isteği ve 52,63 saniyelik kullanıcı kaydı işlendi. Son L2 koşumu mevcut `scripts/stack.sh --mode local exec -T backend` üzerinden çalışan çıkarım servisine gerçek HTTP çağrısı yaptı; [dağıtılmış servis kanıtı](deployed-critical-memory-results.json). Hashler, anonim kararlar ve süreler: [dual-coherence-results.json](dual-coherence-results.json); ham kanıtlar `outputs/2026-09-11-accuracy-audit/` altında korunur.
3. Maddeler:
   **KUSUR**
   M1 İki farklı ses içeren mevcut kalıcı örnek, WeSpeaker küme merkezleri 0,625568 benzerlik verdiği için önceki kontrolden geçiyordu. Bağımsız ECAPA merkezleri 0,541786 verir; kabul edilmiş Decision 24'ün ikinci kontrolü bu örneği reddeder (`meeting_coherence.py`, `meeting_memory.py`).
   M2 Aynı gerçek kaynak isteği önce `usable` ve eski saklanan PCM özetiyle sonuçlandı; yeni özel API yanıtı HTTP 200/`inconsistent_audio` oldu. Saklanacak hash, her iki vektör, ses aralıkları ve kabul edilen bağlamlar boş; süre ve pencere sayısı 0'dır.
   M3 İlk 85 üretim kontrolü geçtikten sonra araştırma istemcisine sentetik anahtarın `SecretStr` nesnesi verilmesi HTTP gönderiminden önce hata üretti. İlk runner/protokol/sonuçlar korundu; ayrı tamamlayıcı runner yalnız test başlığını düzeltti ve 85 GPU kontrolünü tekrar etmedi.
   **TUZAK**
   M4 Modellerin vektörleri ve benzerlikleri birleştirilmez. Önce WeSpeaker çalışır; onun reddi korunur, yalnız izin verdiği örnek ECAPA ile ayrıca denetlenir. İki model de aynı PCM, VAD, tekrar eleme, pencere ve bağımsız destek kurallarını kullanır.
   M5 Bu 85 kayıt gözlenmiş gerileme kümesidir; 85 farklı kişi veya kör değerlendirme değildir. Yeni temiz kayıtlar en az %99 kaynak sahipliğiyle seçilmiştir; bu ölçü elle işaretlenmiş sözcük/fonem saflığı değildir.
   M6 Eski tam istek ölçümü doğrudan Python, yeni ölçüm ASGI/HTTP taşıması içerir; süreler eşit taşımalı bir hız karşılaştırması değildir. İsteğe bağlı decoder ve kernel önbelleği uyarıları saklandı; ses mevcut PCM decoder'ıyla işlendi.
   **GÖZLEM**
   M7 Gerçek üretim fonksiyonu 76 temiz kaydın tamamına izin verdi, 9 karışık kaydın tamamını reddetti; 85 kararın tümü önceden dondurulmuş bağımsız tanıyla aynıydı. Yanlış veto ve kaçan bilinen karışık kayıt 0; hiçbir başarısız örnek çıkarılmadı veya eşik ayarlanmadı.
   M8 85 kontrolde 4.389 WeSpeaker ve 3.946 ECAPA penceresi işlendi; mevcut WeSpeaker reddinden sonra gereksiz ECAPA çağrısı yapılmadı. Kontrollerin toplam süresi 72,25 saniye; bunun 30,51 saniyesi WeSpeaker aşamasıdır. GPU ayrılmış bellek tepesi 154,22 MiB'dir; bütün hassasiyet ayarları korundu.
   M9 Gerçek karışık hafıza isteği eski yolda 3,87 saniye, yeni API yolunda 4,48 saniye sürdü; yeni yolda son vektörler hesaplanmadığından GPU ayrılmış bellek tepesi 175,60 MiB oldu. Yeni API sonrasında kabul kilidinin yeniden alınabilmesi de sınandı.
   M10 Kullanıcının daha önce başarılı olan 52,629375 saniyelik kaydı ayrı kontrolde iki modelden de geçti: 93+93 pencere, 1,47 saniye. Bu kayıt 85 paydasına eklenmedi; kimlik, transkript veya genel Türkçe doğruluğu ölçülmedi.
   M11 Ayrı L2 koşumunda çalışan `d14a4e65…` imajına backend üzerinden gerçek ağ/HTTP isteği yapıldı: HTTP 200/`inconsistent_audio`, 5,39 saniye, saklanabilir çıktı yok. Önce/sonra konteyner kimliği ve başlangıç zamanı aynı; 10 canlı kaynak hash'i imajla eşleşti. [Dağıtılmış servis kanıtı](deployed-critical-memory-results.json).
   **AÇIK**
   M12 Taze galerideki 50 kişilik uçtan uca saflık kök teslim akışında doğrulanır; özel HTTP başarısı bu ölçümü tamamlamaz. Readiness model kimliği tek başına eski/yeni Fa.15 kalite kodunu ayırmadığından M11 kaynak/imaj eşleşmesiyle bağlandı. Kısa kirlenmeler ve ölçülmemiş toplantı akustiği için kusursuzluk iddiası yoktur.
   **YAN-ETKİ**
   M13 Yalnız kabul edilmiş 192/256 boyutlarını destekleyen saf kalite yordamı, final PCM üzerinde ikinci bağımsız veto, sınır testleri ve kanıt dosyaları eklendi. Model dosyası, bağımlılık, eşik, ayar, ASR, veritabanı veya özel HTTP biçimi değişmedi. Kök ajanın imajı etkinleştirmesinden sonra M11 yalnız mevcut özel çıkarım ucunu çağırdı; yeni iş/profil yazmadı. Tam proje kalite kapısını kök ajan yürütür.
