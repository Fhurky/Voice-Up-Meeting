<?xml version="1.0" encoding="utf-8"?><testsuites name="pytest tests"><testsuite name="pytest" errors="0" failures="8" skipped="0" tests="8" time="1.079" timestamp="2026-09-10T12:12:40.421622+03:00" hostname="DESKTOP-RJ78JKH"><testcase classname="tests.test_local_configuration" name="test_real_compose_local_admin_settings_remain_local_and_inherited[default-local]" time="0.123"><failure message="KeyError: 'VOICEUP_LOCAL_ADMIN_LOGIN_ENABLED'">tmp_path = WindowsPath('C:/Users/furko/AppData/Local/Temp/pytest-of-furko/pytest-177/test_real_compose_local_admin_0')
mode = 'local', explicit = False

    @pytest.mark.parametrize("mode", ["local", "spark", "x86_64", "aarch64"])
    @pytest.mark.parametrize("explicit", [False, True], ids=["default", "disabled-with-user"])
    def test_real_compose_local_admin_settings_remain_local_and_inherited(
        tmp_path: Path, mode: str, explicit: bool
    ) -&gt; None:
        docker = shutil.which("docker")
        assert docker, "Docker CLI is required for the local admin Compose contract"
        infra = SCRIPTS.parent / "app/infra"
        env_file = tmp_path / "fixture.env"
        fixture = (
            "INFERENCE_INTERNAL_KEY=fixture-inference-" + "x" * 32 + "\n"
            "RUNTIME_DATABASE_PASSWORD=fixture-runtime-" + "x" * 32 + "\n"
            "GRAFANA_ADMIN_PASSWORD=fixture-grafana\n"
        )
        if mode in {"x86_64", "aarch64"}:
            fixture += f"CPU_ARCH={mode}\n"
        if explicit:
            fixture += "LOCAL_ADMIN_LOGIN_ENABLED=false\nLOCAL_ADMIN_USERNAME=fixture-admin\n"
        env_file.write_text(fixture, encoding="utf-8")
        command = [
            docker,
            "compose",
            "--project-directory",
            str(infra),
            "--env-file",
            str(env_file),
            "-p",
            "voiceup-local-admin-contract",
            "-f",
            str(infra / "docker-compose.local.yml"),
            "-f",
            str(infra / "docker-compose.observability.yml"),
        ]
        if mode != "local":
            layer = "spark" if mode == "spark" else "cpu"
            command += ["-f", str(infra / f"docker-compose.{layer}.yml")]
        environment = {
            key: value
            for key, value in os.environ.items()
            if not key.startswith(("COMPOSE_", "LOCAL_ADMIN_"))
            and key
            not in {
                "CPU_ARCH",
                "INFERENCE_INTERNAL_KEY",
                "RUNTIME_DATABASE_PASSWORD",
                "GRAFANA_ADMIN_PASSWORD",
            }
        }
        result = subprocess.run(
            [*command, "--profile", "*", "config", "--format", "json"],
            env=environment,
            capture_output=True,
            text=True,
            check=False,
            timeout=30,
        )
        assert result.returncode == 0, "Compose contract failed; resolved values withheld"
        services = json.loads(result.stdout)["services"]
        for name in ("backend", "worker", "migrate"):
            configured = services[name]["environment"]
&gt;           assert configured["VOICEUP_LOCAL_ADMIN_LOGIN_ENABLED"] == (
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
                "false" if explicit else "true"
            )
E           KeyError: 'VOICEUP_LOCAL_ADMIN_LOGIN_ENABLED'

tests\test_local_configuration.py:156: KeyError</failure></testcase><testcase classname="tests.test_local_configuration" name="test_real_compose_local_admin_settings_remain_local_and_inherited[default-spark]" time="0.123"><failure message="KeyError: 'VOICEUP_LOCAL_ADMIN_LOGIN_ENABLED'">tmp_path = WindowsPath('C:/Users/furko/AppData/Local/Temp/pytest-of-furko/pytest-177/test_real_compose_local_admin_1')
mode = 'spark', explicit = False

    @pytest.mark.parametrize("mode", ["local", "spark", "x86_64", "aarch64"])
    @pytest.mark.parametrize("explicit", [False, True], ids=["default", "disabled-with-user"])
    def test_real_compose_local_admin_settings_remain_local_and_inherited(
        tmp_path: Path, mode: str, explicit: bool
    ) -&gt; None:
        docker = shutil.which("docker")
        assert docker, "Docker CLI is required for the local admin Compose contract"
        infra = SCRIPTS.parent / "app/infra"
        env_file = tmp_path / "fixture.env"
        fixture = (
            "INFERENCE_INTERNAL_KEY=fixture-inference-" + "x" * 32 + "\n"
            "RUNTIME_DATABASE_PASSWORD=fixture-runtime-" + "x" * 32 + "\n"
            "GRAFANA_ADMIN_PASSWORD=fixture-grafana\n"
        )
        if mode in {"x86_64", "aarch64"}:
            fixture += f"CPU_ARCH={mode}\n"
        if explicit:
            fixture += "LOCAL_ADMIN_LOGIN_ENABLED=false\nLOCAL_ADMIN_USERNAME=fixture-admin\n"
        env_file.write_text(fixture, encoding="utf-8")
        command = [
            docker,
            "compose",
            "--project-directory",
            str(infra),
            "--env-file",
            str(env_file),
            "-p",
            "voiceup-local-admin-contract",
            "-f",
            str(infra / "docker-compose.local.yml"),
            "-f",
            str(infra / "docker-compose.observability.yml"),
        ]
        if mode != "local":
            layer = "spark" if mode == "spark" else "cpu"
            command += ["-f", str(infra / f"docker-compose.{layer}.yml")]
        environment = {
            key: value
            for key, value in os.environ.items()
            if not key.startswith(("COMPOSE_", "LOCAL_ADMIN_"))
            and key
            not in {
                "CPU_ARCH",
                "INFERENCE_INTERNAL_KEY",
                "RUNTIME_DATABASE_PASSWORD",
                "GRAFANA_ADMIN_PASSWORD",
            }
        }
        result = subprocess.run(
            [*command, "--profile", "*", "config", "--format", "json"],
            env=environment,
            capture_output=True,
            text=True,
            check=False,
            timeout=30,
        )
        assert result.returncode == 0, "Compose contract failed; resolved values withheld"
        services = json.loads(result.stdout)["services"]
        for name in ("backend", "worker", "migrate"):
            configured = services[name]["environment"]
&gt;           assert configured["VOICEUP_LOCAL_ADMIN_LOGIN_ENABLED"] == (
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
                "false" if explicit else "true"
            )
E           KeyError: 'VOICEUP_LOCAL_ADMIN_LOGIN_ENABLED'

tests\test_local_configuration.py:156: KeyError</failure></testcase><testcase classname="tests.test_local_configuration" name="test_real_compose_local_admin_settings_remain_local_and_inherited[default-x86_64]" time="0.120"><failure message="KeyError: 'VOICEUP_LOCAL_ADMIN_LOGIN_ENABLED'">tmp_path = WindowsPath('C:/Users/furko/AppData/Local/Temp/pytest-of-furko/pytest-177/test_real_compose_local_admin_2')
mode = 'x86_64', explicit = False

    @pytest.mark.parametrize("mode", ["local", "spark", "x86_64", "aarch64"])
    @pytest.mark.parametrize("explicit", [False, True], ids=["default", "disabled-with-user"])
    def test_real_compose_local_admin_settings_remain_local_and_inherited(
        tmp_path: Path, mode: str, explicit: bool
    ) -&gt; None:
        docker = shutil.which("docker")
        assert docker, "Docker CLI is required for the local admin Compose contract"
        infra = SCRIPTS.parent / "app/infra"
        env_file = tmp_path / "fixture.env"
        fixture = (
            "INFERENCE_INTERNAL_KEY=fixture-inference-" + "x" * 32 + "\n"
            "RUNTIME_DATABASE_PASSWORD=fixture-runtime-" + "x" * 32 + "\n"
            "GRAFANA_ADMIN_PASSWORD=fixture-grafana\n"
        )
        if mode in {"x86_64", "aarch64"}:
            fixture += f"CPU_ARCH={mode}\n"
        if explicit:
            fixture += "LOCAL_ADMIN_LOGIN_ENABLED=false\nLOCAL_ADMIN_USERNAME=fixture-admin\n"
        env_file.write_text(fixture, encoding="utf-8")
        command = [
            docker,
            "compose",
            "--project-directory",
            str(infra),
            "--env-file",
            str(env_file),
            "-p",
            "voiceup-local-admin-contract",
            "-f",
            str(infra / "docker-compose.local.yml"),
            "-f",
            str(infra / "docker-compose.observability.yml"),
        ]
        if mode != "local":
            layer = "spark" if mode == "spark" else "cpu"
            command += ["-f", str(infra / f"docker-compose.{layer}.yml")]
        environment = {
            key: value
            for key, value in os.environ.items()
            if not key.startswith(("COMPOSE_", "LOCAL_ADMIN_"))
            and key
            not in {
                "CPU_ARCH",
                "INFERENCE_INTERNAL_KEY",
                "RUNTIME_DATABASE_PASSWORD",
                "GRAFANA_ADMIN_PASSWORD",
            }
        }
        result = subprocess.run(
            [*command, "--profile", "*", "config", "--format", "json"],
            env=environment,
            capture_output=True,
            text=True,
            check=False,
            timeout=30,
        )
        assert result.returncode == 0, "Compose contract failed; resolved values withheld"
        services = json.loads(result.stdout)["services"]
        for name in ("backend", "worker", "migrate"):
            configured = services[name]["environment"]
&gt;           assert configured["VOICEUP_LOCAL_ADMIN_LOGIN_ENABLED"] == (
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
                "false" if explicit else "true"
            )
E           KeyError: 'VOICEUP_LOCAL_ADMIN_LOGIN_ENABLED'

tests\test_local_configuration.py:156: KeyError</failure></testcase><testcase classname="tests.test_local_configuration" name="test_real_compose_local_admin_settings_remain_local_and_inherited[default-aarch64]" time="0.116"><failure message="KeyError: 'VOICEUP_LOCAL_ADMIN_LOGIN_ENABLED'">tmp_path = WindowsPath('C:/Users/furko/AppData/Local/Temp/pytest-of-furko/pytest-177/test_real_compose_local_admin_3')
mode = 'aarch64', explicit = False

    @pytest.mark.parametrize("mode", ["local", "spark", "x86_64", "aarch64"])
    @pytest.mark.parametrize("explicit", [False, True], ids=["default", "disabled-with-user"])
    def test_real_compose_local_admin_settings_remain_local_and_inherited(
        tmp_path: Path, mode: str, explicit: bool
    ) -&gt; None:
        docker = shutil.which("docker")
        assert docker, "Docker CLI is required for the local admin Compose contract"
        infra = SCRIPTS.parent / "app/infra"
        env_file = tmp_path / "fixture.env"
        fixture = (
            "INFERENCE_INTERNAL_KEY=fixture-inference-" + "x" * 32 + "\n"
            "RUNTIME_DATABASE_PASSWORD=fixture-runtime-" + "x" * 32 + "\n"
            "GRAFANA_ADMIN_PASSWORD=fixture-grafana\n"
        )
        if mode in {"x86_64", "aarch64"}:
            fixture += f"CPU_ARCH={mode}\n"
        if explicit:
            fixture += "LOCAL_ADMIN_LOGIN_ENABLED=false\nLOCAL_ADMIN_USERNAME=fixture-admin\n"
        env_file.write_text(fixture, encoding="utf-8")
        command = [
            docker,
            "compose",
            "--project-directory",
            str(infra),
            "--env-file",
            str(env_file),
            "-p",
            "voiceup-local-admin-contract",
            "-f",
            str(infra / "docker-compose.local.yml"),
            "-f",
            str(infra / "docker-compose.observability.yml"),
        ]
        if mode != "local":
            layer = "spark" if mode == "spark" else "cpu"
            command += ["-f", str(infra / f"docker-compose.{layer}.yml")]
        environment = {
            key: value
            for key, value in os.environ.items()
            if not key.startswith(("COMPOSE_", "LOCAL_ADMIN_"))
            and key
            not in {
                "CPU_ARCH",
                "INFERENCE_INTERNAL_KEY",
                "RUNTIME_DATABASE_PASSWORD",
                "GRAFANA_ADMIN_PASSWORD",
            }
        }
        result = subprocess.run(
            [*command, "--profile", "*", "config", "--format", "json"],
            env=environment,
            capture_output=True,
            text=True,
            check=False,
            timeout=30,
        )
        assert result.returncode == 0, "Compose contract failed; resolved values withheld"
        services = json.loads(result.stdout)["services"]
        for name in ("backend", "worker", "migrate"):
            configured = services[name]["environment"]
&gt;           assert configured["VOICEUP_LOCAL_ADMIN_LOGIN_ENABLED"] == (
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
                "false" if explicit else "true"
            )
E           KeyError: 'VOICEUP_LOCAL_ADMIN_LOGIN_ENABLED'

tests\test_local_configuration.py:156: KeyError</failure></testcase><testcase classname="tests.test_local_configuration" name="test_real_compose_local_admin_settings_remain_local_and_inherited[disabled-with-user-local]" time="0.114"><failure message="KeyError: 'VOICEUP_LOCAL_ADMIN_LOGIN_ENABLED'">tmp_path = WindowsPath('C:/Users/furko/AppData/Local/Temp/pytest-of-furko/pytest-177/test_real_compose_local_admin_4')
mode = 'local', explicit = True

    @pytest.mark.parametrize("mode", ["local", "spark", "x86_64", "aarch64"])
    @pytest.mark.parametrize("explicit", [False, True], ids=["default", "disabled-with-user"])
    def test_real_compose_local_admin_settings_remain_local_and_inherited(
        tmp_path: Path, mode: str, explicit: bool
    ) -&gt; None:
        docker = shutil.which("docker")
        assert docker, "Docker CLI is required for the local admin Compose contract"
        infra = SCRIPTS.parent / "app/infra"
        env_file = tmp_path / "fixture.env"
        fixture = (
            "INFERENCE_INTERNAL_KEY=fixture-inference-" + "x" * 32 + "\n"
            "RUNTIME_DATABASE_PASSWORD=fixture-runtime-" + "x" * 32 + "\n"
            "GRAFANA_ADMIN_PASSWORD=fixture-grafana\n"
        )
        if mode in {"x86_64", "aarch64"}:
            fixture += f"CPU_ARCH={mode}\n"
        if explicit:
            fixture += "LOCAL_ADMIN_LOGIN_ENABLED=false\nLOCAL_ADMIN_USERNAME=fixture-admin\n"
        env_file.write_text(fixture, encoding="utf-8")
        command = [
            docker,
            "compose",
            "--project-directory",
            str(infra),
            "--env-file",
            str(env_file),
            "-p",
            "voiceup-local-admin-contract",
            "-f",
            str(infra / "docker-compose.local.yml"),
            "-f",
            str(infra / "docker-compose.observability.yml"),
        ]
        if mode != "local":
            layer = "spark" if mode == "spark" else "cpu"
            command += ["-f", str(infra / f"docker-compose.{layer}.yml")]
        environment = {
            key: value
            for key, value in os.environ.items()
            if not key.startswith(("COMPOSE_", "LOCAL_ADMIN_"))
            and key
            not in {
                "CPU_ARCH",
                "INFERENCE_INTERNAL_KEY",
                "RUNTIME_DATABASE_PASSWORD",
                "GRAFANA_ADMIN_PASSWORD",
            }
        }
        result = subprocess.run(
            [*command, "--profile", "*", "config", "--format", "json"],
            env=environment,
            capture_output=True,
            text=True,
            check=False,
            timeout=30,
        )
        assert result.returncode == 0, "Compose contract failed; resolved values withheld"
        services = json.loads(result.stdout)["services"]
        for name in ("backend", "worker", "migrate"):
            configured = services[name]["environment"]
&gt;           assert configured["VOICEUP_LOCAL_ADMIN_LOGIN_ENABLED"] == (
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
                "false" if explicit else "true"
            )
E           KeyError: 'VOICEUP_LOCAL_ADMIN_LOGIN_ENABLED'

tests\test_local_configuration.py:156: KeyError</failure></testcase><testcase classname="tests.test_local_configuration" name="test_real_compose_local_admin_settings_remain_local_and_inherited[disabled-with-user-spark]" time="0.119"><failure message="KeyError: 'VOICEUP_LOCAL_ADMIN_LOGIN_ENABLED'">tmp_path = WindowsPath('C:/Users/furko/AppData/Local/Temp/pytest-of-furko/pytest-177/test_real_compose_local_admin_5')
mode = 'spark', explicit = True

    @pytest.mark.parametrize("mode", ["local", "spark", "x86_64", "aarch64"])
    @pytest.mark.parametrize("explicit", [False, True], ids=["default", "disabled-with-user"])
    def test_real_compose_local_admin_settings_remain_local_and_inherited(
        tmp_path: Path, mode: str, explicit: bool
    ) -&gt; None:
        docker = shutil.which("docker")
        assert docker, "Docker CLI is required for the local admin Compose contract"
        infra = SCRIPTS.parent / "app/infra"
        env_file = tmp_path / "fixture.env"
        fixture = (
            "INFERENCE_INTERNAL_KEY=fixture-inference-" + "x" * 32 + "\n"
            "RUNTIME_DATABASE_PASSWORD=fixture-runtime-" + "x" * 32 + "\n"
            "GRAFANA_ADMIN_PASSWORD=fixture-grafana\n"
        )
        if mode in {"x86_64", "aarch64"}:
            fixture += f"CPU_ARCH={mode}\n"
        if explicit:
            fixture += "LOCAL_ADMIN_LOGIN_ENABLED=false\nLOCAL_ADMIN_USERNAME=fixture-admin\n"
        env_file.write_text(fixture, encoding="utf-8")
        command = [
            docker,
            "compose",
            "--project-directory",
            str(infra),
            "--env-file",
            str(env_file),
            "-p",
            "voiceup-local-admin-contract",
            "-f",
            str(infra / "docker-compose.local.yml"),
            "-f",
            str(infra / "docker-compose.observability.yml"),
        ]
        if mode != "local":
            layer = "spark" if mode == "spark" else "cpu"
            command += ["-f", str(infra / f"docker-compose.{layer}.yml")]
        environment = {
            key: value
            for key, value in os.environ.items()
            if not key.startswith(("COMPOSE_", "LOCAL_ADMIN_"))
            and key
            not in {
                "CPU_ARCH",
                "INFERENCE_INTERNAL_KEY",
                "RUNTIME_DATABASE_PASSWORD",
                "GRAFANA_ADMIN_PASSWORD",
            }
        }
        result = subprocess.run(
            [*command, "--profile", "*", "config", "--format", "json"],
            env=environment,
            capture_output=True,
            text=True,
            check=False,
            timeout=30,
        )
        assert result.returncode == 0, "Compose contract failed; resolved values withheld"
        services = json.loads(result.stdout)["services"]
        for name in ("backend", "worker", "migrate"):
            configured = services[name]["environment"]
&gt;           assert configured["VOICEUP_LOCAL_ADMIN_LOGIN_ENABLED"] == (
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
                "false" if explicit else "true"
            )
E           KeyError: 'VOICEUP_LOCAL_ADMIN_LOGIN_ENABLED'

tests\test_local_configuration.py:156: KeyError</failure></testcase><testcase classname="tests.test_local_configuration" name="test_real_compose_local_admin_settings_remain_local_and_inherited[disabled-with-user-x86_64]" time="0.133"><failure message="KeyError: 'VOICEUP_LOCAL_ADMIN_LOGIN_ENABLED'">tmp_path = WindowsPath('C:/Users/furko/AppData/Local/Temp/pytest-of-furko/pytest-177/test_real_compose_local_admin_6')
mode = 'x86_64', explicit = True

    @pytest.mark.parametrize("mode", ["local", "spark", "x86_64", "aarch64"])
    @pytest.mark.parametrize("explicit", [False, True], ids=["default", "disabled-with-user"])
    def test_real_compose_local_admin_settings_remain_local_and_inherited(
        tmp_path: Path, mode: str, explicit: bool
    ) -&gt; None:
        docker = shutil.which("docker")
        assert docker, "Docker CLI is required for the local admin Compose contract"
        infra = SCRIPTS.parent / "app/infra"
        env_file = tmp_path / "fixture.env"
        fixture = (
            "INFERENCE_INTERNAL_KEY=fixture-inference-" + "x" * 32 + "\n"
            "RUNTIME_DATABASE_PASSWORD=fixture-runtime-" + "x" * 32 + "\n"
            "GRAFANA_ADMIN_PASSWORD=fixture-grafana\n"
        )
        if mode in {"x86_64", "aarch64"}:
            fixture += f"CPU_ARCH={mode}\n"
        if explicit:
            fixture += "LOCAL_ADMIN_LOGIN_ENABLED=false\nLOCAL_ADMIN_USERNAME=fixture-admin\n"
        env_file.write_text(fixture, encoding="utf-8")
        command = [
            docker,
            "compose",
            "--project-directory",
            str(infra),
            "--env-file",
            str(env_file),
            "-p",
            "voiceup-local-admin-contract",
            "-f",
            str(infra / "docker-compose.local.yml"),
            "-f",
            str(infra / "docker-compose.observability.yml"),
        ]
        if mode != "local":
            layer = "spark" if mode == "spark" else "cpu"
            command += ["-f", str(infra / f"docker-compose.{layer}.yml")]
        environment = {
            key: value
            for key, value in os.environ.items()
            if not key.startswith(("COMPOSE_", "LOCAL_ADMIN_"))
            and key
            not in {
                "CPU_ARCH",
                "INFERENCE_INTERNAL_KEY",
                "RUNTIME_DATABASE_PASSWORD",
                "GRAFANA_ADMIN_PASSWORD",
            }
        }
        result = subprocess.run(
            [*command, "--profile", "*", "config", "--format", "json"],
            env=environment,
            capture_output=True,
            text=True,
            check=False,
            timeout=30,
        )
        assert result.returncode == 0, "Compose contract failed; resolved values withheld"
        services = json.loads(result.stdout)["services"]
        for name in ("backend", "worker", "migrate"):
            configured = services[name]["environment"]
&gt;           assert configured["VOICEUP_LOCAL_ADMIN_LOGIN_ENABLED"] == (
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
                "false" if explicit else "true"
            )
E           KeyError: 'VOICEUP_LOCAL_ADMIN_LOGIN_ENABLED'

tests\test_local_configuration.py:156: KeyError</failure></testcase><testcase classname="tests.test_local_configuration" name="test_real_compose_local_admin_settings_remain_local_and_inherited[disabled-with-user-aarch64]" time="0.114"><failure message="KeyError: 'VOICEUP_LOCAL_ADMIN_LOGIN_ENABLED'">tmp_path = WindowsPath('C:/Users/furko/AppData/Local/Temp/pytest-of-furko/pytest-177/test_real_compose_local_admin_7')
mode = 'aarch64', explicit = True

    @pytest.mark.parametrize("mode", ["local", "spark", "x86_64", "aarch64"])
    @pytest.mark.parametrize("explicit", [False, True], ids=["default", "disabled-with-user"])
    def test_real_compose_local_admin_settings_remain_local_and_inherited(
        tmp_path: Path, mode: str, explicit: bool
    ) -&gt; None:
        docker = shutil.which("docker")
        assert docker, "Docker CLI is required for the local admin Compose contract"
        infra = SCRIPTS.parent / "app/infra"
        env_file = tmp_path / "fixture.env"
        fixture = (
            "INFERENCE_INTERNAL_KEY=fixture-inference-" + "x" * 32 + "\n"
            "RUNTIME_DATABASE_PASSWORD=fixture-runtime-" + "x" * 32 + "\n"
            "GRAFANA_ADMIN_PASSWORD=fixture-grafana\n"
        )
        if mode in {"x86_64", "aarch64"}:
            fixture += f"CPU_ARCH={mode}\n"
        if explicit:
            fixture += "LOCAL_ADMIN_LOGIN_ENABLED=false\nLOCAL_ADMIN_USERNAME=fixture-admin\n"
        env_file.write_text(fixture, encoding="utf-8")
        command = [
            docker,
            "compose",
            "--project-directory",
            str(infra),
            "--env-file",
            str(env_file),
            "-p",
            "voiceup-local-admin-contract",
            "-f",
            str(infra / "docker-compose.local.yml"),
            "-f",
            str(infra / "docker-compose.observability.yml"),
        ]
        if mode != "local":
            layer = "spark" if mode == "spark" else "cpu"
            command += ["-f", str(infra / f"docker-compose.{layer}.yml")]
        environment = {
            key: value
            for key, value in os.environ.items()
            if not key.startswith(("COMPOSE_", "LOCAL_ADMIN_"))
            and key
            not in {
                "CPU_ARCH",
                "INFERENCE_INTERNAL_KEY",
                "RUNTIME_DATABASE_PASSWORD",
                "GRAFANA_ADMIN_PASSWORD",
            }
        }
        result = subprocess.run(
            [*command, "--profile", "*", "config", "--format", "json"],
            env=environment,
            capture_output=True,
            text=True,
            check=False,
            timeout=30,
        )
        assert result.returncode == 0, "Compose contract failed; resolved values withheld"
        services = json.loads(result.stdout)["services"]
        for name in ("backend", "worker", "migrate"):
            configured = services[name]["environment"]
&gt;           assert configured["VOICEUP_LOCAL_ADMIN_LOGIN_ENABLED"] == (
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
                "false" if explicit else "true"
            )
E           KeyError: 'VOICEUP_LOCAL_ADMIN_LOGIN_ENABLED'

tests\test_local_configuration.py:156: KeyError</failure></testcase></testsuite></testsuites>