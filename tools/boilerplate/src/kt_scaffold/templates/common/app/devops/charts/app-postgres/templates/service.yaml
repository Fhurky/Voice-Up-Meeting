apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  labels: {app.kubernetes.io/part-of: "@@PRODUCT_SLUG@@", app.kubernetes.io/component: postgres}
spec:
  type: ClusterIP
  clusterIP: None
  selector: {app.kubernetes.io/part-of: "@@PRODUCT_SLUG@@", app.kubernetes.io/component: postgres}
  ports:
    - {name: postgres, port: 5432, targetPort: postgres}

