# Plans

This directory contains pre-decision working documents. Once a plan is accepted, its durable product
contract belongs in specs/<domain>/PRDs/. Split scope becomes a separate PRD; it is not silently
parked.

