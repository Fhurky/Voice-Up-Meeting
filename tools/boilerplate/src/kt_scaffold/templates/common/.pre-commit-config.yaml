repos:
  - repo: local
    hooks:
      - id: gitleaks
        name: gitleaks offline secret scan
        entry: gitleaks dir --config .gitleaks.toml --redact --exit-code 1 .
        language: system
        pass_filenames: false
