# Set the locally verified immutable sha256:<64 hex> image ID; no pull is allowed.
SPARK_INFERENCE_IMAGE=
# Copy only the matching inference key using a private channel, at least 32 bytes.
INFERENCE_INTERNAL_KEY=
